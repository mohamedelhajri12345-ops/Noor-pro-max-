import React,{useState} from 'react';
export default function NoorAI(){const [q,setQ]=useState('');const [status,setStatus]=useState('');const ask=()=>{if(!q.trim())return;setStatus('لم يتم ربط مزود AI بعد. لن يعرض Noor إجابة دينية غير موثقة.');};return <section className="page fade-in"><div className="page-title"><span>Noor AI</span><small>مساعد إسلامي Online فقط</small></div><div className="ai-card"><div className="ai-icon">✦</div><h2>اسأل وتحقق قبل العمل</h2><p>هذه الخدمة منفصلة عن وظائف Noor. عند ربط مزود AI، يجب إظهار مصادر الآيات والأحاديث ودرجة الحديث عند توفرها والتنبيه إلى الخلاف الفقهي.</p><div className="notice">حاليًا المساعد غير متصل بنموذج حقيقي، لذلك لن نخترع إجابات أو أحاديث.</div><div className="ai-form"><input className="ai-input" value={q} onChange={e=>setQ(e.target.value)} placeholder="اكتب سؤالك…"/><button className="secondary" onClick={ask}>إرسال</button></div>{status&&<p className="muted">{status}</p>}</div></section>}

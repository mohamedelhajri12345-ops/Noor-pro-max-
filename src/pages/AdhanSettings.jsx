import React,{useState} from 'react';
export default function AdhanSettings(){const [enabled,setEnabled]=useState(localStorage.getItem('noor.adhan')!=='off');const [volume,setVolume]=useState(localStorage.getItem('noor.volume')||'70');return <section className="page fade-in"><div className="page-title"><span>إعدادات الأذان</span><small>تحكم محلي بإشعارات الأذان</small></div><div className="settings-card"><label className="setting-row"><span>الأذان</span><input type="checkbox" checked={enabled} onChange={e=>{setEnabled(e.target.checked);localStorage.setItem('noor.adhan',e.target.checked?'on':'off')}} /></label><label className="setting-row"><span>الصوت {volume}%</span><input type="range" min="0" max="100" value={volume} onChange={e=>{setVolume(e.target.value);localStorage.setItem('noor.volume',e.target.value)}} /></label><p className="muted">سيطلب Noor صلاحيات الإشعارات والموقع قبل استخدامها، ويعتمد على قيود النظام للأذان في الخلفية.</p></div></section>}

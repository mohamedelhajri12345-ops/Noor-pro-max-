import React,{useEffect,useState} from 'react';
const toRad=d=>d*Math.PI/180; const toDeg=r=>r*180/Math.PI;
function qiblaBearing(lat,lon){const ka=toRad(21.422487), ko=toRad(39.826206), p=toRad(lat), l=toRad(lon);return (toDeg(Math.atan2(Math.sin(ko-l),Math.cos(p)*Math.tan(ka)-Math.sin(p)*Math.cos(ko-l)))+360)%360}
export default function Qibla(){const [angle,setAngle]=useState(null);const [bearing,setBearing]=useState(null);const [error,setError]=useState('');useEffect(()=>{if(navigator.geolocation)navigator.geolocation.getCurrentPosition(p=>setBearing(qiblaBearing(p.coords.latitude,p.coords.longitude)),()=>setError('اسمح بالموقع لحساب اتجاه القبلة من موقعك.'));const handler=e=>{let a=e.webkitCompassHeading;if(typeof a!=='number')a=typeof e.alpha==='number'?360-e.alpha:null;if(a!==null)setAngle(a);};window.addEventListener('deviceorientation',handler,true);return()=>window.removeEventListener('deviceorientation',handler,true)},[]);const direction=bearing===null?null:Math.round((bearing-(angle||0)+360)%360);return <section className="page center-page fade-in"><div className="page-title"><span>القبلة</span><small>اتجاه مكة المكرمة</small></div><div className="compass"><div className="needle" style={{transform:`translate(-50%,-50%) rotate(${direction||0}deg)`}}>▲</div><span className="north">N</span><span className="qibla-label">🕋</span></div><strong>{bearing===null?'جاري تحديد موقعك…':`اتجاه القبلة ${Math.round(bearing)}°`}</strong><p>{error||(!angle?'حرّك الهاتف برفق لمعايرة البوصلة.':`اتجاه الجهاز ${Math.round(angle)}°`)}</p></section>}
